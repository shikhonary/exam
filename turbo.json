{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"],
      "env": [
        "DATABASE_URL",
        "TENANT_DATABASE_URL",
        "RESEND_API_KEY",
        "BETTER_AUTH_SECRET",
        "BETTER_AUTH_URL",
        "NEXT_PUBLIC_APP_URL",
        "GEMINI_API_KEY",
        "INNGEST_EVENT_KEY",
        "INNGEST_SIGNING_KEY",
        "GOOGLE_TTS_API_KEY",
        "NODE_ENV"
      ]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "env": [
        "DATABASE_URL",
        "TENANT_DATABASE_URL",
        "RESEND_API_KEY",
        "BETTER_AUTH_SECRET",
        "BETTER_AUTH_URL",
        "GEMINI_API_KEY",
        "INNGEST_EVENT_KEY",
        "INNGEST_SIGNING_KEY",
        "GOOGLE_TTS_API_KEY",
        "NODE_ENV"
      ],
      "dependsOn": ["^db:generate"],
      "cache": false,
      "persistent": true
    },
    "db:generate": {
      "cache": false
    },
    "db:generate:tenant": {
      "cache": false
    },
    "db:generate:all": {
      "dependsOn": ["db:generate", "db:generate:tenant"],
      "cache": false
    }
  }
}
